# Local-only migration notes (pre-rename installs) — never published
/MIGRATION.md

# Ephemeral session handoff documents — never commit
handoff.md

# OS / editor clutter
.DS_Store
*.swp
.idea/
.vscode/
